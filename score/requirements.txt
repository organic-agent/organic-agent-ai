# score — 사진별 점수 Lambda (CPU). 로컬 venv 는 score/.venv (wes scripts/lib/ai-venv.sh 가 만든다).
# torch·torchvision 은 Dockerfile 이 CPU 인덱스로 따로 깐다(CUDA 빌드가 딸려오지 않게). 로컬(맥)은 여기 핀을 그대로 쓴다.
torch==2.4.1
torchvision==0.19.1
open_clip_torch==2.26.1
numpy==1.26.4
# classical 선명도(Laplacian) — categorize 와 같은 핀
scipy==1.14.1
pillow==10.4.0
pillow-heif==0.18.0
# ARNIQA torch.hub 의존
dotmap==1.3.30
# DB · S3 · 체인(Lambda invoke)
psycopg[binary]==3.2.3
pgvector==0.3.6
boto3==1.35.24
# 테스트
pytest==8.3.3
