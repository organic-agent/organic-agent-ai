# photoselect — 로컬 실행·배치용 (CPU). torch 는 SCORE(runners/)만 쓴다. 로컬 venv 는 photoselect/.venv
# (wes scripts/local-worker.sh 가 만든다). Lambda 컨테이너로 갈 때는 embedder 처럼 CPU 인덱스로 분리한다.
torch==2.4.1
torchvision==0.19.1
open_clip_torch==2.26.1
numpy==1.26.4
scipy==1.14.1
pillow==10.4.0
pillow-heif==0.18.0
# ARNIQA torch.hub 의존
dotmap==1.3.30
# naming — Bedrock 구조화 출력. SDK 1.x (output_config, aws_region 필수)
anthropic[bedrock]>=1,<2
# 테스트
pytest==8.3.3
# DB 모드 (--db). 로컬 파일 모드만 쓰면 없어도 된다.
psycopg[binary]==3.2.3
pgvector==0.3.6
boto3==1.35.24
