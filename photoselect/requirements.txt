# photoselect — 로컬 실행·A 배치용 (CPU). GPU 이미지는 requirements-gpu.txt로 분리 예정.
# 스파이크 하네스(scripts/spike/.venv)와 같은 핀이라 그 venv로도 그대로 돈다.
torch==2.4.1
torchvision==0.19.1
open_clip_torch==2.26.1
mediapipe==0.10.14
numpy==1.26.4
scipy==1.14.1
pillow==10.4.0
pillow-heif==0.18.0
# ARNIQA torch.hub 의존
dotmap==1.3.30
# 테스트
pytest==8.3.3
# DB 모드 (wes V22 후). 로컬 모드는 없어도 된다.
# psycopg[binary]==3.2.3
# pgvector==0.3.6
# boto3==1.35.24
