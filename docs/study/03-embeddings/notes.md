# 03 임베딩과 표현학습 — 노트

이미 OpenCLIP·DINOv2·HDBSCAN 파이프라인을 만들어봤으므로, 실습이 아니라 **원리**를 채운다.
`/study-quiz 03`이 이 파일을 출제 근거로 쓴다.

## 개념 체크리스트

- [ ] 임베딩과 유사도 — 코사인 vs 유클리드, 고차원 공간에서 유사도의 의미
- [ ] CLIP(대조학습) vs DINOv2(자기지도) — 학습 방식 차이가 만드는 강점 차이,
      "왜 태그(VLM)와 벡터(DINOv2)를 둘 다 쓰는가"
- [ ] 클러스터링 — 밀도 기반(HDBSCAN) vs 중심 기반(k-means)
- [ ] 선형대수 최소치 — 벡터, 내적, 코사인 유사도, 차원

## 정리

<!-- 개념별로: ## 개념명 / 내 설명 / 설계안 연결 -->
